# Настройка CI/CD Pipeline

Этот документ описывает настройку автоматического CI/CD pipeline для проекта Media Archive.

## Архитектура CI/CD

Проект использует GitHub Actions для автоматизации процессов разработки и деплоя. Pipeline состоит из трех основных workflows:

### 1. CI Pipeline (`ci.yml`)

Выполняется при каждом push и pull request. Включает:

- ✅ **Code Quality Checks** - проверка форматирования, линтинг, проверка типов
- 🧪 **Tests** - запуск тестов с покрытием кода
- 📦 **Build** - сборка всех пакетов (server, client)
- 🔒 **Security** - проверка безопасности зависимостей и секретов
- 🐳 **Docker Build** - сборка Docker образов (только для main/master)

### 2. CD Pipeline (`cd.yml`)

Запускается автоматически после успешного завершения CI Pipeline на ветках `main`/`master`, или вручную через `workflow_dispatch`. Включает:

- ✅ Проверку статуса CI
- 🚀 Деплой на VPS сервер
- 🏥 Health checks после деплоя
- 📊 Детальные отчеты о деплое
- 🔄 Автоматический откат при ошибках

### 3. Security Scan (`security.yml`)

Выполняется при push, pull request и еженедельно по расписанию:

- 🔍 Сканирование уязвимостей зависимостей (npm audit, Snyk)
- 🔐 Поиск секретов в коде (TruffleHog, Gitleaks)
- 🐳 Сканирование Docker образов (Trivy)
- ⚙️ Проверка конфигурации на безопасность

## Настройка секретов в GitHub

Для работы CI/CD pipeline нужно настроить секреты в GitHub:

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Добавьте следующие секреты:

### Обязательные секреты для деплоя:

- **`VPS_HOST`** - IP адрес или доменное имя вашего VPS сервера
  - Пример: `123.45.67.89` или `server.example.com`

- **`VPS_USER`** - Имя пользователя для SSH подключения
  - Пример: `root` или `deploy`

- **`VPS_SSH_KEY`** - Приватный SSH ключ для подключения к серверу
  - Содержимое файла `~/.ssh/id_rsa` (или другого приватного ключа)
  - Важно: используйте ключ без пароля или настройте SSH agent

### Опциональные секреты:

- **`VPS_SSH_PORT`** - Порт SSH, по умолчанию 22
  - Если используете стандартный порт 22, можно не указывать

- **`FRONTEND_URL`** - URL вашего фронтенда для отображения в GitHub
  - Пример: `https://xn----8sbgxcrot9b.xn--p1ai`

- **`API_BASE_URL`** - URL вашего API
  - Пример: `https://api.example.com`

- **`VITE_GOOGLE_CLIENT_ID`** - Google Client ID для клиента (используется при сборке)

- **`CODECOV_TOKEN`** - Токен для загрузки coverage отчетов (опционально)
  - Получить на https://codecov.io

- **`SNYK_TOKEN`** - Токен для Snyk security scanning (опционально)
  - Получить на https://snyk.io

- **`TELEGRAM_BOT_TOKEN`** и **`TELEGRAM_CHAT_ID`** - для уведомлений в Telegram (опционально)

## Генерация SSH ключа

Если у вас еще нет SSH ключа:

```bash
# Генерируем новый ключ (если еще нет)
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_deploy

# Копируем публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_deploy.pub user@your-server-ip

# Показываем приватный ключ для добавления в GitHub Secrets
cat ~/.ssh/github_deploy
```

**Важно:** Скопируйте весь вывод команды `cat`, включая строки `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`

## Настройка сервера

Убедитесь, что на сервере:

1. Установлен Git и настроен доступ к репозиторию:

```bash
cd /opt/media-archive/archive
git remote -v  # Должен показывать ваш репозиторий
```

2. Если репозиторий приватный, настройте SSH ключ для Git:

```bash
# На сервере
ssh-keygen -t ed25519 -C "server-deploy"
cat ~/.ssh/id_ed25519.pub
# Добавьте этот ключ в GitHub: Settings → SSH and GPG keys → New SSH key
```

3. Убедитесь, что пользователь имеет права на Docker:

```bash
# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER
# Перелогиньтесь или выполните:
newgrp docker
```

4. Проверьте, что директория существует и доступна:

```bash
ls -la /opt/media-archive/archive
# Должна быть видна директория с проектом
```

5. Убедитесь, что на сервере установлены необходимые инструменты:

```bash
# Docker и Docker Compose
docker --version
docker compose version

# Git
git --version

# jq для парсинга JSON (используется в health checks)
sudo apt-get install jq  # для Debian/Ubuntu
# или
sudo yum install jq      # для CentOS/RHEL
```

## Как это работает

### Автоматический деплой

1. При каждом push в ветку `main` или `master`:
   - Запускается **CI Pipeline** - проверка кода, тесты, сборка
   - После успешного завершения CI автоматически запускается **CD Pipeline**
   - Выполняется деплой на VPS сервер
   - Проводятся health checks
   - При ошибке выполняется автоматический откат

2. При создании Pull Request:
   - Запускается только **CI Pipeline** (без деплоя)
   - Проверяется качество кода, тесты, сборка
   - Результаты отображаются в PR

### Ручной запуск деплоя

Если нужно запустить деплой вручную:

1. Перейдите в GitHub → **Actions**
2. Выберите workflow **"CD Pipeline"**
3. Нажмите **Run workflow**
4. Выберите:
   - Ветку (обычно `main`)
   - Окружение (`production` или `staging`)
5. Нажмите **Run workflow**

## Мониторинг и отчеты

### В GitHub Actions

В логах GitHub Actions вы увидите:

- ✅ Успешные шаги
- ❌ Ошибки (если есть)
- 📊 Статус контейнеров
- 🏥 Результаты проверки здоровья сервисов
- 📈 Coverage отчеты
- 🔒 Результаты security сканирования

### Coverage отчеты

Coverage отчеты автоматически:

- Загружаются как артефакты (доступны 30 дней)
- Комментируются в Pull Request (если настроен токен)
- Могут быть отправлены в Codecov (если настроен токен)

### Security отчеты

Security сканирование:

- Выполняется автоматически при каждом push/PR
- Запускается еженедельно по расписанию
- Результаты загружаются как артефакты
- Критические уязвимости отображаются в GitHub Security tab

## Решение проблем

### CI Pipeline не проходит

#### Ошибки линтинга

```bash
# Локально проверьте и исправьте
npm run lint
npm run format
```

#### Ошибки тестов

```bash
# Запустите тесты локально
npm run test
```

#### Ошибки сборки

```bash
# Проверьте сборку локально
npm run build
```

### CD Pipeline не проходит

#### Ошибка подключения по SSH

- Проверьте правильность `VPS_HOST` и `VPS_USER`
- Убедитесь, что SSH ключ правильный и добавлен на сервер
- Проверьте, что порт SSH открыт в firewall
- Проверьте, что сервер доступен из интернета

#### Ошибка "git pull failed"

- Убедитесь, что на сервере настроен доступ к репозиторию
- Проверьте права доступа к директории `/opt/media-archive/archive`
- Если репозиторий приватный, добавьте SSH ключ сервера в GitHub

#### Ошибка при сборке Docker

- Проверьте логи в GitHub Actions
- Убедитесь, что на сервере достаточно места: `df -h`
- Проверьте, что Docker работает: `docker ps`
- Проверьте логи на сервере: `docker compose -f docker-compose.prod.yml logs`

#### Контейнеры не запускаются

- Проверьте переменные окружения в `.env` на сервере
- Посмотрите логи: `docker compose -f docker-compose.prod.yml logs`
- Убедитесь, что порты не заняты: `netstat -tulpn | grep -E '80|443|4000'`
- Проверьте health checks: `docker compose -f docker-compose.prod.yml ps`

#### Health checks не проходят

- Проверьте, что API доступен: `curl http://localhost:4000/api/health`
- Проверьте логи контейнеров на сервере
- Убедитесь, что база данных запущена и доступна
- Проверьте сетевые настройки Docker

### Откат изменений

Если деплой прошел неудачно, можно откатить вручную:

```bash
# На сервере
cd /opt/media-archive/archive
git checkout <предыдущий-коммит>
docker compose -f docker-compose.prod.yml up -d --build
```

Или используйте автоматический откат, который выполняется при ошибке деплоя.

## Безопасность

⚠️ **Важно:**

- Никогда не коммитьте секреты в репозиторий
- Используйте GitHub Secrets для всех чувствительных данных
- Регулярно обновляйте SSH ключи
- Используйте отдельного пользователя для деплоя (не root)
- Настройте firewall на сервере
- Регулярно проверяйте security отчеты
- Обновляйте зависимости с уязвимостями

## Дополнительные настройки

### Уведомления о деплое в Telegram

Для настройки уведомлений в Telegram:

1. **Создайте бота через [@BotFather](https://t.me/BotFather)**
   - Отправьте команду `/newbot`
   - Следуйте инструкциям и получите токен бота

2. **Получите chat ID**
   - Напишите боту любое сообщение
   - Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   - Найдите в ответе `"chat":{"id":123456789}` - это ваш chat ID
   - Или используйте [@userinfobot](https://t.me/userinfobot) для получения вашего ID

3. **Добавьте секреты в GitHub:**
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота
   - `TELEGRAM_CHAT_ID` - ваш chat ID
   - `TELEGRAM_DEPLOY_SECRET` - секретный токен для защиты API endpoint (сгенерируйте случайную строку)

4. **Настройте переменные окружения на сервере:**
   - `TELEGRAM_BOT_TOKEN` - токен бота
   - `TELEGRAM_CHAT_ID` - chat ID
   - `TELEGRAM_DEPLOY_SECRET` - тот же секрет, что и в GitHub (для защиты endpoint)

После настройки уведомления будут автоматически отправляться:
- ✅ При успешном деплое
- ❌ При ошибке деплоя
- 🆕 При регистрации новых пользователей

**Примечание:** Если переменные не настроены, уведомления просто не будут отправляться, но это не повлияет на работу приложения.

### Настройка Codecov

1. Зарегистрируйтесь на https://codecov.io
2. Подключите ваш GitHub репозиторий
3. Получите токен
4. Добавьте секрет `CODECOV_TOKEN` в GitHub

### Настройка Snyk

1. Зарегистрируйтесь на https://snyk.io
2. Подключите ваш GitHub репозиторий
3. Получите токен
4. Добавьте секрет `SNYK_TOKEN` в GitHub

## Лучшие практики

✅ **Используйте feature branches** - создавайте PR для всех изменений
✅ **Проверяйте CI перед мержем** - убедитесь, что все проверки прошли
✅ **Пишите тесты** - увеличивайте coverage кода
✅ **Регулярно обновляйте зависимости** - исправляйте уязвимости
✅ **Мониторьте логи** - проверяйте результаты деплоя
✅ **Используйте environments** - настройте отдельные окружения для staging/production

❌ **Не коммитьте секреты** - используйте GitHub Secrets
❌ **Не игнорируйте security warnings** - исправляйте уязвимости
❌ **Не деплойте без тестов** - убедитесь, что все тесты проходят
❌ **Не используйте root для деплоя** - создайте отдельного пользователя

## Структура workflows

```
.github/workflows/
├── ci.yml          # CI Pipeline - проверка кода, тесты, сборка
├── cd.yml          # CD Pipeline - деплой на сервер
└── security.yml     # Security Scan - проверка безопасности
```

## Дополнительная информация

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Docker Best Practices](https://docs.docker.com/develop/dev-best-practices/)
- [Security Best Practices](https://docs.github.com/en/code-security)
