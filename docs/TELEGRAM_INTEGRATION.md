# Интеграция с Telegram

Модуль Telegram уведомлений позволяет получать уведомления о важных событиях в вашем приложении.

## Возможности

- ✅ Уведомления о деплое (успешном или неудачном)
- 🆕 Уведомления о регистрации новых пользователей
- 📊 Готовность к расширению для статистики и мониторинга

## Архитектура модуля

Модуль состоит из нескольких слоев:

```
server/src/
├── infrastructure/integrations/
│   ├── telegram.client.ts      # Клиент для работы с Telegram Bot API
│   └── telegram.service.ts      # Сервис для форматирования и отправки уведомлений
├── application/telegram/
│   └── telegram.service.ts      # Application слой (бизнес-логика)
└── app/
    ├── controllers/
    │   └── telegram.controller.ts  # HTTP контроллер
    └── routes/
        └── telegram.routes.ts     # Маршруты API
```

## Настройка

### 1. Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота

### 2. Получение Chat ID

**Способ 1: Через API**

1. Напишите вашему боту любое сообщение
2. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
3. Найдите в ответе `"chat":{"id":123456789}` - это ваш chat ID

**Способ 2: Через бота**

1. Используйте [@userinfobot](https://t.me/userinfobot)
2. Отправьте команду `/start`
3. Бот вернет ваш ID

### 3. Настройка переменных окружения

Добавьте следующие переменные в `.env` файл на сервере:

```bash
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
TELEGRAM_DEPLOY_SECRET=your_random_secret_here  # Для защиты API endpoint
```

### 4. Настройка GitHub Secrets

Для уведомлений о деплое добавьте секреты в GitHub:

1. Перейдите в **Settings** → **Secrets and variables** → **Actions**
2. Добавьте:
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота
   - `TELEGRAM_CHAT_ID` - ваш chat ID
   - `TELEGRAM_DEPLOY_SECRET` - секретный токен (должен совпадать с `TELEGRAM_DEPLOY_SECRET` на сервере)
   - `API_BASE_URL` - URL вашего API (например, `https://api.example.com`)

## Использование

### Уведомления о деплое

Уведомления о деплое отправляются автоматически через GitHub Actions workflow после завершения деплоя.

**Endpoint:** `POST /api/telegram/deploy`

**Защита:** Bearer токен (TELEGRAM_DEPLOY_SECRET)

**Пример запроса:**

```bash
curl -X POST "https://api.example.com/api/telegram/deploy" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_DEPLOY_SECRET" \
  -d '{
    "status": "success",
    "branch": "main",
    "commit": "abc123",
    "author": "username",
    "environment": "production",
    "duration": 120
  }'
```

### Уведомления о регистрации

Уведомления о регистрации новых пользователей отправляются автоматически при создании нового аккаунта.

### Программное использование

```typescript
import { container } from './app/container.js';

// Отправка произвольного сообщения
await container.telegram.notificationService.sendMessage('Привет! Это тестовое сообщение', {
  parseMode: 'HTML',
});

// Отправка уведомления о деплое
await container.telegram.notificationService.notifyDeployment({
  status: 'success',
  branch: 'main',
  commit: 'abc123',
  author: 'username',
  environment: 'production',
  duration: 120,
});

// Отправка уведомления о новом пользователе
await container.telegram.notificationService.notifyNewUser({
  userId: 1,
  email: 'user@example.com',
  name: 'John Doe',
});
```

## Безопасность

- Endpoint для уведомлений о деплое защищен Bearer токеном
- Токен должен быть достаточно сложным (рекомендуется минимум 32 символа)
- Не храните секреты в коде, используйте переменные окружения
- В продакшене используйте HTTPS для всех запросов

## Расширение функциональности

Модуль готов к расширению для:

- 📊 Статистики по сайту (количество пользователей, посетителей)
- 📈 Мониторинга метрик
- 🔔 Других типов уведомлений

Для добавления новой функциональности:

1. Добавьте метод в `TelegramService` (infrastructure слой)
2. Добавьте метод в `TelegramNotificationService` (application слой)
3. При необходимости создайте новый endpoint в контроллере

## Примеры форматирования сообщений

Модуль поддерживает HTML форматирование:

```typescript
// Жирный текст
<b>Текст</b>

// Моноширинный текст
<code>код</code>

// Эмодзи
✅ ❌ 🆕 📊 📈 🔔
```

## Отладка

Если уведомления не приходят:

1. Проверьте, что переменные окружения установлены:

   ```bash
   echo $TELEGRAM_BOT_TOKEN
   echo $TELEGRAM_CHAT_ID
   ```

2. Проверьте логи сервера на наличие ошибок

3. Убедитесь, что бот активен и может отправлять сообщения

4. Проверьте, что chat ID правильный (отправьте тестовое сообщение боту)

5. Для уведомлений о деплое проверьте:
   - Правильность `API_BASE_URL` в GitHub Secrets
   - Совпадение `TELEGRAM_DEPLOY_SECRET` в GitHub и на сервере
   - Доступность API endpoint с GitHub Actions

## Отключение уведомлений

Если нужно временно отключить уведомления, просто не устанавливайте переменные окружения. Модуль автоматически определит отсутствие конфигурации и не будет пытаться отправлять сообщения.
