# Настройка деплоя на Railway

Railway - это платформа для деплоя приложений с бесплатным тарифом, которая предоставляет глобальный URL для вашего приложения.

## Быстрый старт

**Вариант 1: Все на Railway**
1. Создайте аккаунт на https://railway.app/
2. Создайте проект и подключите GitHub репозиторий
3. Добавьте PostgreSQL базу данных
4. Создайте сервис для сервера
5. (Опционально) Создайте сервис для клиента
6. Настройте переменные окружения
7. Получите URL в настройках сервисов

**Вариант 2: Railway + Vercel (рекомендуется, проще)**
1. Сервер на Railway (с PostgreSQL)
2. Клиент на Vercel (бесплатно, автоматический деплой)
3. Настройте переменные окружения

**Результат:** Ваше приложение будет доступно по глобальному URL

## Шаги настройки

### 1. Создайте аккаунт на Railway

1. Перейдите на https://railway.app/
2. Зарегистрируйтесь через GitHub (рекомендуется)
3. Подтвердите email

### 2. Создайте новый проект

1. В панели Railway нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий `media-archive`
4. Railway автоматически обнаружит ваш проект

### 3. Настройте сервисы

**Шаг 1: Добавить PostgreSQL базу данных**
1. В проекте нажмите "+ New" (или "New" → "Database")
2. Выберите "PostgreSQL"
3. Railway автоматически создаст базу данных
4. В настройках базы данных (Settings → Variables) вы увидите переменные:
   - `PGHOST`, `PGPORT`, `PGUSER`, `PGPASSWORD`, `PGDATABASE`
   - Запомните их или скопируйте

**Шаг 2: Создать сервис для сервера**
1. Нажмите "+ New" → "GitHub Repo"
2. Выберите ваш репозиторий `media-archive`
3. Railway начнет деплой
4. После создания сервиса:
   - Нажмите на сервис
   - Settings → Name: переименуйте в `server` (опционально)
   - Settings → Source: убедитесь, что указан правильный репозиторий

**Шаг 3: Настроить Dockerfile для сервера**
1. В настройках сервиса `server` → Settings → Source
2. **ВАЖНО:** Убедитесь, что:
   - **Root Directory:** пусто (или `/`) - **должен быть корень проекта, не `server/`!**
   - **Dockerfile Path:** `server/Dockerfile`
3. Если Root Directory установлен в `server/`, измените на пустое значение или `/`
4. Это критично, так как Dockerfile копирует файлы из `shared/` и `server/`, которые находятся в корне проекта

**Шаг 4: Создать сервис для клиента (опционально, можно позже)**
1. Нажмите "+ New" → "GitHub Repo"
2. Выберите тот же репозиторий `media-archive`
3. После создания:
   - Settings → Name: переименуйте в `client`
   - Settings → Source:
     - **Root Directory:** пусто (или `/`) - **должен быть корень проекта, не `client/`!**
     - **Dockerfile Path:** `client/Dockerfile.prod`
4. Это критично, так как Dockerfile.prod копирует файлы из `shared/` и `client/`, которые находятся в корне проекта

**💡 Альтернативный вариант - деплой клиента на Vercel (проще):**

Если не получается создать второй сервис в Railway, можно:
1. Задеплоить только сервер на Railway
2. Задеплоить клиент на Vercel (бесплатно, очень просто)
3. Vercel автоматически обнаружит React приложение и задеплоит его

**Инструкция для Vercel:**
1. Перейдите на https://vercel.com/
2. Войдите через GitHub
3. "Add New Project" → выберите ваш репозиторий `media-archive`
4. В настройках проекта:
   - **Framework Preset:** Vite (или оставьте Auto)
   - **Root Directory:** `client`
   - **Build Command:** `npm run build` (или оставьте по умолчанию)
   - **Output Directory:** `dist`
5. Environment Variables (добавьте после первого деплоя, когда получите URL сервера):
   - `API_BASE_URL` = URL вашего Railway сервера (например: `https://your-server.up.railway.app`)
   - `FRONTEND_URL` = URL вашего Vercel приложения (будет после деплоя)
6. Нажмите "Deploy"

После деплоя Vercel даст вам URL вида `your-app.vercel.app`

**Важно:** После первого деплоя обновите переменные окружения в Vercel с реальными URL.

### 4. Получите Railway Token

1. Перейдите в настройки проекта: Settings → Tokens
2. Нажмите "New Token"
3. Скопируйте токен (он показывается только один раз!)

### 5. Добавьте секреты в GitHub

1. Перейдите в ваш репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Добавьте секрет:
   - **Имя:** `RAILWAY_TOKEN`
   - **Значение:** токен, который вы скопировали на шаге 4

### 6. Свяжите PostgreSQL с сервисом server

**ВАЖНО:** Railway не автоматически связывает базу данных с сервисами. Нужно сделать это вручную.

1. Перейдите в ваш проект Railway
2. Нажмите на сервис `server`
3. Перейдите в Settings → Variables
4. Найдите раздел "Reference Variables" или "Connect Database"
5. Нажмите "Connect" или "Add Reference" рядом с вашей PostgreSQL базой данных
6. Railway автоматически добавит переменные: `PGHOST`, `PGPORT`, `PGUSER`, `PGPASSWORD`, `PGDATABASE`

**Альтернативный способ (если нет кнопки Connect):**
1. Перейдите в настройки PostgreSQL базы данных → Variables
2. Скопируйте значения переменных: `PGHOST`, `PGPORT`, `PGUSER`, `PGPASSWORD`, `PGDATABASE`
3. Перейдите в настройки сервиса `server` → Variables
4. Добавьте эти переменные вручную

### 7. Настройте остальные переменные окружения

**Для сервиса `server`:**

1. Перейдите в настройки сервиса → Variables
2. Убедитесь, что переменные PostgreSQL уже добавлены (из шага 6)
3. Добавьте остальные переменные:
   ```
   NODE_ENV=production
   PORT=4000
   JWT_SECRET=<ваш секретный ключ, минимум 32 символа>
   JWT_REFRESH_SECRET=<ваш секретный ключ, минимум 32 символа>
   ACCESS_TOKEN_TTL_MINUTES=15
   REFRESH_TOKEN_TTL_DAYS=30
   KINOPOISK_API_KEY=<ваш API ключ Кинопоиска>
   GOOGLE_CLIENT_ID=<ваш Google Client ID>
   FRONTEND_URL=<URL вашего фронтенда, будет после деплоя>
   ```
   
   **Примечание:** `API_BASE_URL` не нужен для сервера (сервер сам является API). Он нужен только для клиента.

**Для сервиса `client` (если используете Railway для клиента):**

1. Перейдите в настройки сервиса → Variables
2. Добавьте переменные:
   ```
   API_BASE_URL=<URL вашего API сервера>
   FRONTEND_URL=<URL вашего фронтенда>
   ```

**Важно:** URL для `API_BASE_URL` и `FRONTEND_URL` вы получите после первого деплоя (шаг 8). Сначала можно оставить пустыми или использовать временные значения.

### 8. Настройте Railway для деплоя

Railway может деплоить двумя способами:

#### Вариант A: Автоматический деплой из GitHub (рекомендуется)

1. В настройках проекта Railway включите "Auto Deploy"
2. Railway будет автоматически деплоить при каждом push в main ветку
3. Railway автоматически обнаружит Dockerfile и соберет образ
4. В этом случае GitHub Actions workflow не нужен

#### Вариант B: Деплой через GitHub Actions

1. Используйте созданный workflow `.github/workflows/railway-deploy.yml`
2. Он будет запускаться при каждом push в main
3. Убедитесь, что `RAILWAY_TOKEN` добавлен в GitHub Secrets
4. Этот вариант полезен, если нужны дополнительные шаги перед деплоем

### 9. Получите URL вашего приложения

После первого деплоя:

**Для сервера (API):**

1. Перейдите в ваш проект Railway
2. Откройте сервис `server`
3. В разделе **"Settings"** → **"Networking"** нажмите **"Generate Domain"**
4. Railway создаст публичный URL вида: `your-server-name.up.railway.app`
5. Скопируйте URL сервера (например: `https://server-production-6c01.up.railway.app`)

**Для клиента (фронтенд) - это ваш фронтенд!**

1. В том же проекте Railway откройте сервис `client`
2. В разделе **"Settings"** → **"Networking"** нажмите **"Generate Domain"**
3. Railway создаст публичный URL вида: `your-client-name.up.railway.app`
4. Скопируйте URL клиента (например: `https://client-production-xxxx.up.railway.app`)

**🎉 Это и есть URL вашего фронтенда!** Откройте его в браузере, чтобы увидеть ваше приложение.

**Настройка переменных окружения:**

После получения URL обновите переменные окружения:

1. **В сервисе `server`** → Settings → Variables:
   - Установите `FRONTEND_URL` = URL клиента (например: `https://client-production-xxxx.up.railway.app`)

2. **В сервисе `client`** → Settings → Variables:
   - **ВАЖНО:** Установите `API_BASE_URL` = URL сервера (например: `https://server-production-6c01.up.railway.app`)
   - Установите `FRONTEND_URL` = URL клиента (например: `https://client-production-xxxx.up.railway.app`)
   - **КРИТИЧНО:** Переменная `API_BASE_URL` должна быть установлена ДО сборки. После добавления переменной Railway автоматически пересоберет и перезапустит сервис.

3. После обновления переменных Railway автоматически перезапустит сервисы

### 10. Миграции базы данных

**✅ Хорошая новость:** Миграции запускаются автоматически при каждом старте сервера!

При деплое сервера миграции выполняются автоматически. Вы можете проверить это в логах сервера - там должны быть сообщения вида:
```
[migrate] applied migration_name.sql
```

**Если нужно запустить миграции вручную (например, если они не запустились автоматически):**

**Способ 1: Через Railway CLI**

1. Установите Railway CLI:
   ```bash
   npm i -g @railway/cli
   ```

2. Войдите в Railway:
   ```bash
   railway login
   ```

3. Подключитесь к вашему проекту:
   ```bash
   railway link
   ```
   Выберите ваш проект из списка

4. Запустите миграции (команда выполнится на Railway, не локально):
   ```bash
   railway run --service server node dist/db/migrate.js
   ```
   
   **Важно:** Эта команда выполняется внутри контейнера на Railway, а не на вашей локальной машине. Убедитесь, что вы подключены к правильному проекту через `railway link`.

**Способ 2: Через Railway Dashboard**

1. В Railway перейдите в ваш проект
2. Откройте сервис `server`
3. Перейдите в раздел "Deployments"
4. Найдите последний деплой и откройте его
5. В разделе "Run Command" введите:
   ```
   node dist/db/migrate.js
   ```
6. Нажмите "Run"

**Способ 3: Локальный запуск миграций (для тестирования)**

Если вы хотите запустить миграции локально (например, для тестирования):

1. Убедитесь, что у вас запущена локальная база данных PostgreSQL
2. Соберите проект:
   ```bash
   npm -w shared run build
   npm -w server run build
   ```
3. Установите переменные окружения для подключения к БД (или используйте `.env` файл)
4. Запустите миграции:
   ```bash
   cd server
   node dist/db/migrate.js
   ```

**Проверка успешности миграций:**

Проверьте логи сервера в Railway. Если миграции выполнены успешно, вы увидите сообщения:
- `[migrate] applied <имя_миграции>.sql` - миграция применена
- Если есть ошибки, они будут в логах

**Важно:** Команда `railway run` выполняется на Railway, а не на вашей локальной машине. Если вы видите ошибку "Cannot find module", это значит, что команда пытается выполниться локально. Убедитесь, что вы:
1. Выполнили `railway link` и выбрали правильный проект
2. Команда `railway run` действительно выполняется на Railway (вы увидите это в выводе)

### 11. Настройте кастомный домен (опционально)

1. В настройках сервиса → Networking
2. Добавьте свой домен
3. Настройте DNS записи согласно инструкциям Railway

## Важные замечания

1. **Бесплатный тариф:**
   - 500 часов работы в месяц
   - $5 кредитов в месяц
   - После исчерпания сервисы останавливаются

2. **База данных:**
   - Railway предоставляет бесплатную PostgreSQL базу данных
   - Данные хранятся пока проект активен
   - Рекомендуется делать бэкапы

3. **Переменные окружения:**
   - Railway автоматически предоставляет переменные для подключения к БД
   - Используйте их вместо ручного ввода

4. **Миграции:**
   - Миграции можно запустить через Railway CLI или вручную
   - Или добавить команду запуска миграций в Dockerfile

## Устранение проблем

### Ошибка: `"/server/src": not found` при сборке

**Проблема:** Railway не может найти файлы при сборке Docker образа.

**Решение:**
1. Перейдите в настройки сервиса → Settings → Source
2. Убедитесь, что **Root Directory** пустой (или `/`), а не `server/`
3. **Dockerfile Path** должен быть `server/Dockerfile`
4. Сохраните изменения и перезапустите деплой

**Почему это важно:** Dockerfile копирует файлы из `shared/` и `server/`, которые находятся в корне проекта. Если Root Directory установлен в `server/`, Railway не сможет найти файлы из `shared/`.

### Ошибка: `"/client/nginx.conf": not found` при сборке клиента

**Проблема:** Railway не может найти файлы при сборке Docker образа для клиента.

**Решение:**
1. Перейдите в настройки сервиса `client` → Settings → Source
2. Убедитесь, что **Root Directory** пустой (или `/`), а не `client/`
3. **Dockerfile Path** должен быть `client/Dockerfile.prod`
4. Сохраните изменения и перезапустите деплой

**Почему это важно:** Dockerfile.prod копирует файлы из `shared/` и `client/`, которые находятся в корне проекта. Если Root Directory установлен в `client/`, Railway не сможет найти файлы из `shared/` и `nginx.conf`.

### Ошибка: `API_BASE_URL is required`

**Проблема:** Сервер не запускается из-за отсутствия `API_BASE_URL`.

**Решение:** `API_BASE_URL` не нужен для сервера! Убедитесь, что вы не установили эту переменную для сервера. Она нужна только для клиента.

### Ошибка 502 "connection refused" на сервере

**Проблема:** Railway не может подключиться к контейнеру сервера. Сервер либо не запустился, либо упал после запуска.

**Решение:**

1. **Проверьте логи деплоя сервера:**
   - В Railway откройте сервис `server`
   - Перейдите в раздел "Deploy Logs" или "Deployments"
   - Найдите последний деплой и откройте его
   - Посмотрите логи - там должна быть причина ошибки

2. **Проверьте логи запуска (Deploy Logs):**
   - Ищите ошибки типа:
     - `Failed to start server`
     - `Error connecting to database`
     - `Cannot find module`
     - `API_BASE_URL is required`

3. **Проверьте переменные окружения:**
   - Убедитесь, что все необходимые переменные установлены
   - Особенно переменные для подключения к PostgreSQL (PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE)
   - Убедитесь, что PostgreSQL база данных связана с сервисом

4. **Проверьте, что сервер слушает правильный порт:**
   - В переменных окружения должна быть установлена переменная `PORT=4000` (или другой порт)
   - Или сервер использует порт по умолчанию 4000
   - Railway автоматически определяет порт из переменной `PORT` или из `EXPOSE` в Dockerfile

5. **Частые причины:**
   - Сервер не может подключиться к базе данных (проверьте переменные PostgreSQL)
   - Ошибка при запуске миграций
   - Отсутствуют необходимые переменные окружения
   - Ошибка в коде при инициализации

**Как исправить:**
- Если ошибка в подключении к БД - свяжите PostgreSQL с сервисом (см. раздел выше)
- Если ошибка в переменных - добавьте недостающие переменные
- Если ошибка в коде - проверьте логи и исправьте проблему

### Ошибка: `connect ECONNREFUSED ::1:5432` или `connect ECONNREFUSED 127.0.0.1:5432`

**Проблема:** Сервер не может подключиться к PostgreSQL базе данных.

**Решение:**
1. Убедитесь, что PostgreSQL база данных создана в Railway
2. **Свяжите базу данных с сервисом server:**
   - Перейдите в настройки сервиса `server` → Settings → Variables
   - Найдите раздел "Reference Variables" или "Connect Database"
   - Нажмите "Connect" или "Add Reference" рядом с вашей PostgreSQL базой данных
   - Railway автоматически добавит переменные: `PGHOST`, `PGPORT`, `PGUSER`, `PGPASSWORD`, `PGDATABASE`
3. Если нет кнопки Connect, скопируйте переменные из настроек PostgreSQL базы данных и добавьте их вручную в сервис `server`
4. Перезапустите сервис `server`

### Фронтенд показывает ошибку или не загружается

**Проблема:** Фронтенд не может подключиться к API или показывает ошибку.

**Решение:**

1. **Проверьте переменные окружения в сервисе `client`:**
   - Откройте сервис `client` → Settings → Variables
   - Убедитесь, что установлена переменная `API_BASE_URL` с URL вашего сервера
   - Формат: `https://your-server-name.up.railway.app` (без слеша в конце!)
   - Пример: `https://server-production-6c01.up.railway.app`

2. **Проверьте, что сервер работает:**
   - Откройте URL сервера в браузере
   - Должен быть доступен endpoint `/api/health` или `/health`
   - Если сервер не отвечает, проверьте его логи

3. **Проверьте CORS настройки:**
   - В сервисе `server` → Settings → Variables
   - Убедитесь, что `FRONTEND_URL` установлен на URL вашего клиента
   - Пример: `https://client-production-xxxx.up.railway.app`

4. **Пересоберите клиент после изменения переменных:**
   - После добавления/изменения `API_BASE_URL` Railway автоматически пересоберет клиент
   - Дождитесь завершения деплоя
   - Проверьте логи сборки на наличие ошибок

5. **Проверьте HTTP логи клиента:**
   - В Railway откройте сервис `client` → "HTTP Logs"
   - Посмотрите, какие запросы приходят и какие ошибки возвращаются
   - Ошибки 404 или CORS обычно указывают на неправильный `API_BASE_URL`

**Частые ошибки:**
- `Failed to fetch` или `Network error` - неправильный `API_BASE_URL` или сервер недоступен
- `CORS error` - неправильный `FRONTEND_URL` на сервере
- Белый экран - ошибка JavaScript, проверьте консоль браузера (F12)

## Полезные команды Railway CLI

```bash
# Установка CLI
npm i -g @railway/cli

# Логин
railway login

# Подключение к проекту
railway link

# Просмотр логов
railway logs

# Запуск локально с переменными из Railway
railway run npm start
```

## Альтернативные платформы

Если Railway не подходит, рассмотрите:
- **Render** - похожий функционал, бесплатный тариф
- **Fly.io** - хорошая поддержка Docker
- **Vercel** - отлично для фронтенда, но нужен отдельный сервер для API

