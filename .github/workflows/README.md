# CI/CD Workflows

Этот каталог содержит GitHub Actions workflows для автоматизации процессов разработки и деплоя.

## Workflows

### 🔍 CI Pipeline (`ci.yml`)

Основной CI pipeline, который выполняется при каждом push и pull request.

**Этапы:**

1. **Code Quality Checks** - проверка форматирования, линтинг, проверка типов TypeScript
2. **Tests** - запуск тестов с coverage отчетами
3. **Build** - сборка всех пакетов (shared, server, client)
4. **Security** - проверка безопасности зависимостей и поиск секретов
5. **Docker Build** - сборка Docker образов (только для main/master)

**Особенности:**

- ✅ Параллельное выполнение независимых задач
- ✅ Кэширование зависимостей npm
- ✅ Coverage отчеты с загрузкой артефактов
- ✅ Автоматическая отмена предыдущих запусков для того же коммита
- ✅ Matrix builds для параллельной сборки пакетов

### 🚀 CD Pipeline (`cd.yml`)

Pipeline для автоматического деплоя на production сервер.

**Триггеры:**

- Автоматически после успешного CI на ветках `main`/`master`
- Вручную через `workflow_dispatch`

**Этапы:**

1. Проверка статуса CI
2. Деплой на VPS через SSH
3. Health checks (контейнеры, API, фронтенд)
4. Автоматический откат при ошибках
5. Уведомления о результате

**Особенности:**

- ✅ Детальное логирование всех шагов
- ✅ Health checks с retry логикой
- ✅ Автоматический откат при ошибках
- ✅ Deployment summary в GitHub
- ✅ Поддержка разных окружений (production/staging)

### 🔒 Security Scan (`security.yml`)

Автоматическое сканирование безопасности проекта.

**Проверки:**

1. **Dependency Scan** - npm audit, Snyk
2. **Secret Scan** - TruffleHog, Gitleaks
3. **Docker Scan** - Trivy для сканирования образов
4. **Config Scan** - проверка конфигурации на безопасность

**Расписание:**

- При каждом push/PR
- Еженедельно (каждый понедельник в 00:00 UTC)

## Улучшения по сравнению с предыдущей версией

### ✨ Новые возможности

1. **Разделение на отдельные jobs** - параллельное выполнение, лучшая видимость
2. **Coverage отчеты** - автоматическая загрузка и отображение в PR
3. **Security scanning** - регулярная проверка уязвимостей
4. **Health checks** - автоматическая проверка работоспособности после деплоя
5. **Автоматический откат** - при ошибках деплоя
6. **Детальное логирование** - подробные отчеты о каждом шаге
7. **Кэширование** - оптимизация времени выполнения
8. **Matrix builds** - параллельная сборка разных пакетов
9. **Type checking** - отдельная проверка типов TypeScript
10. **Format checking** - проверка форматирования кода

### 🔧 Улучшения

- ✅ Улучшенная обработка ошибок
- ✅ Timeout для всех jobs
- ✅ Детальные summary отчеты
- ✅ Поддержка разных окружений
- ✅ Оптимизированное кэширование
- ✅ Лучшая структура и читаемость

### 📊 Метрики и отчеты

- Coverage отчеты загружаются как артефакты
- Security отчеты доступны в GitHub Security tab
- Deployment summary отображается в GitHub Actions
- Детальные логи каждого шага

## Настройка

Подробные инструкции по настройке см. в [docs/CI_CD_SETUP.md](../../docs/CI_CD_SETUP.md)

## Требуемые секреты

- `VPS_HOST` - адрес сервера
- `VPS_USER` - пользователь для SSH
- `VPS_SSH_KEY` - приватный SSH ключ
- `VPS_SSH_PORT` (опционально) - порт SSH
- `FRONTEND_URL` (опционально) - URL фронтенда
- `API_BASE_URL` (опционально) - URL API
- `CODECOV_TOKEN` (опционально) - для coverage отчетов
- `SNYK_TOKEN` (опционально) - для security scanning

## Лучшие практики

✅ Все workflows следуют лучшим практикам:

- Разделение ответственности
- Параллельное выполнение где возможно
- Кэширование зависимостей
- Детальное логирование
- Обработка ошибок
- Security best practices
