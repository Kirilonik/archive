# Список найденных проблем, багов и неточностей

## 🔴 Критические проблемы безопасности

### 1. CSP с unsafe-inline и unsafe-eval в продакшене
**Файл:** `server/src/index.ts:43`
**Проблема:** В Content Security Policy разрешены `'unsafe-inline'` и `'unsafe-eval'`, что снижает защиту от XSS атак.
```typescript
scriptSrc: ["'self'", "'unsafe-inline'", "'unsafe-eval'"], // TODO: убрать unsafe для продакшена
```
**Решение:** Убрать unsafe директивы, использовать nonce или хеши для скриптов.

### 2. CSRF токен доступен через JavaScript
**Файл:** `server/src/middlewares/csrf.ts:15`
**Проблема:** CSRF токен устанавливается с `httpOnly: false`, что делает его уязвимым для XSS атак.
```typescript
res.cookie(CSRF_COOKIE_NAME, token, {
  httpOnly: false,  // ❌ Проблема безопасности
  secure: isProd,
  sameSite: 'lax',
  path: '/',
  maxAge: 7 * 24 * 60 * 60 * 1000,
});
```
**Решение:** Если токен нужен в JavaScript, использовать отдельный механизм (например, meta tag или отдельный endpoint).

### 3. Origin validation middleware пропускает запросы без origin/referer
**Файл:** `server/src/middlewares/origin.ts:34-40`
**Проблема:** Если нет ни `origin`, ни `referer` заголовков, запрос пропускается. Это может позволить обойти защиту.
```typescript
const refererOrigin = extractOrigin(req.headers.referer as string | undefined);
if (refererOrigin && !allowedOrigins.has(refererOrigin)) {
  res.status(403).json({ error: 'Origin not allowed' });
  return;
}

next(); // ❌ Пропускает запросы без origin и referer
```
**Решение:** Для небезопасных методов (POST, PUT, DELETE) требовать наличие валидного origin или referer.

### 4. Отсутствие валидации параметров URL (ID)
**Файлы:** Множество контроллеров
**Проблема:** Использование `Number(req.params.id)` без проверки на `NaN` или отрицательные значения.
```typescript
const film = await this.filmService.getFilm(Number(req.params.id), userId);
// ❌ Если req.params.id = "abc", Number("abc") = NaN
```
**Решение:** Добавить валидацию через Zod или проверку на `isNaN` и положительные значения.

## 🟠 Серьезные проблемы

### 5. Потенциальный бесконечный цикл при refresh токене
**Файл:** `client/src/lib/api.ts:142-152`
**Проблема:** Если `/api/auth/refresh` возвращает 401, может возникнуть бесконечный цикл попыток обновления.
```typescript
if (response.status === 401 && !isAuthEndpoint) {
  const refreshResponse = await fetch(resolveRequestInput('/api/auth/refresh'), {
    method: 'POST',
    credentials: 'include',
    signal: createTimeoutSignal(REQUEST_TIMEOUT),
  });

  if (refreshResponse.ok) {
    response = await doFetch(); // ❌ Может снова вернуть 401
  }
}
```
**Решение:** Добавить флаг, предотвращающий повторные попытки refresh, или проверять статус refresh ответа.

### 6. Дублирование проверки cookies в auth.controller.ts
**Файл:** `server/src/app/controllers/auth.controller.ts:130`
**Проблема:** Избыточная проверка cookies с использованием `(req as any)`.
```typescript
const token = (req as any).cookies?.refresh_token || req.cookies?.refresh_token;
```
**Решение:** Убрать дублирование, использовать только `req.cookies?.refresh_token`.

### 7. Проблема с обработкой сигналов в apiFetch
**Файл:** `client/src/lib/api.ts:111-119`
**Проблема:** Созданный `AbortController` для объединения сигналов не освобождается, возможна утечка памяти.
```typescript
const combinedSignal = userSignal 
  ? (() => {
      const combined = new AbortController();
      const abort = () => combined.abort();
      timeoutSignal.addEventListener('abort', abort);
      userSignal.addEventListener('abort', abort);
      return combined.signal; // ❌ Слушатели не удаляются
    })()
  : timeoutSignal;
```
**Решение:** Сохранять ссылку на controller и очищать слушатели после завершения запроса.

### 8. Отсутствие rate limiting на некоторых эндпоинтах
**Файл:** `server/src/routes/register.ts`
**Проблема:** Не все эндпоинты защищены rate limiting. Например, `/api/users/*` не имеет rate limiter.
**Решение:** Добавить rate limiting на все эндпоинты, особенно на модифицирующие операции.

## 🟡 Средние проблемы

### 9. Использование типа `any` в catch блоках
**Файлы:** Множество контроллеров
**Проблема:** Использование `error: any` снижает типобезопасность и может скрыть ошибки.
```typescript
} catch (error: any) {
  if (error?.status === 409) {
    return res.status(409).json({ error: 'Already exists' });
  }
}
```
**Решение:** Использовать `unknown` и проверять типы явно, или создать кастомные классы ошибок.

### 10. Недостаточная обработка ошибок в миграциях
**Файл:** `server/src/db/migrate.ts`
**Проблема:** При ошибке миграции транзакция откатывается, но нет детального логирования.
**Решение:** Улучшить логирование ошибок миграций с указанием конкретного файла и строки.

### 11. Проблема с валидацией JWT секретов в env.ts
**Файл:** `server/src/config/env.ts:30,46`
**Проблема:** Проверка `process.env.NODE_ENV` внутри refine функции может работать некорректно из-за порядка инициализации.
```typescript
const isProd = process.env.NODE_ENV === 'production';
```
**Решение:** Использовать уже распарсенные значения из схемы, а не напрямую `process.env`.

### 12. Отсутствие валидации размера загружаемых данных
**Файл:** `server/src/infrastructure/media/sharp-avatar-processor.ts`
**Проблема:** Проверка размера происходит только после декодирования base64, что может привести к проблемам с памятью для больших строк.
**Решение:** Проверять размер base64 строки до декодирования.

### 13. Проблема с обработкой ошибок в Home.tsx
**Файл:** `client/src/pages/Home.tsx:163-169`
**Проблема:** При ошибке загрузки все данные сбрасываются, даже если часть данных уже загружена.
```typescript
} catch (error) {
  if (controller.signal.aborted) return;
  console.error('Не удалось загрузить библиотеку', error);
  setItems([]); // ❌ Сбрасывает все данные
  setVisibleCount(0);
  setFilmsState(createPaginationState());
  setSeriesState(createPaginationState());
}
```
**Решение:** Сохранять уже загруженные данные и показывать частичную ошибку.

### 14. Отсутствие проверки на null/undefined в Number() преобразованиях
**Файлы:** Все контроллеры
**Проблема:** `Number(undefined)` возвращает `NaN`, что может привести к неожиданному поведению.
**Решение:** Добавить явную проверку или использовать `z.coerce.number()` из Zod.

## 🔵 Мелкие проблемы и улучшения

### 15. TODO комментарий в продакшн коде
**Файл:** `server/src/index.ts:43`
**Проблема:** TODO комментарий остался в продакшн коде.
**Решение:** Убрать unsafe директивы или перенести TODO в issue tracker.

### 16. Использование `as any` в swagger.ts
**Файл:** `server/src/docs/swagger.ts:48`
**Проблема:** Использование `as any` обходит проверку типов.
```typescript
const spec = swaggerJSDoc(options as any);
```
**Решение:** Исправить типы или использовать правильную типизацию.

### 17. Отсутствие валидации email в некоторых местах
**Проблема:** Хотя есть валидация через Zod, стоит убедиться, что она применяется везде.
**Решение:** Проверить все места, где обрабатывается email.

### 18. Проблема с обработкой пустых ответов
**Файл:** `client/src/lib/api.ts:168`
**Проблема:** При ошибке парсинга JSON возвращается пустой объект, что может скрыть реальную ошибку.
```typescript
const error = await response.json().catch(() => ({}));
```
**Решение:** Логировать ошибку парсинга или использовать более информативное сообщение.

### 19. Отсутствие проверки на существование user перед операциями
**Файлы:** Контроллеры
**Проблема:** В некоторых местах проверка `req.user?.id` происходит, но не везде последовательно.
**Решение:** Убедиться, что `authMiddleware` всегда устанавливает `req.user`, или добавить проверки везде.

### 20. Проблема с таймаутом в graceful shutdown
**Файл:** `server/src/index.ts:101-104`
**Проблема:** Принудительное завершение через 10 секунд может прервать важные операции (например, сохранение данных).
**Решение:** Увеличить таймаут или добавить механизм ожидания завершения критических операций.

### 21. Отсутствие валидации на максимальную длину строк в некоторых полях
**Файл:** `server/src/app/validators/films.schema.ts`
**Проблема:** Некоторые поля имеют очень большие лимиты (например, `opinion: max(50000)`.
**Решение:** Пересмотреть лимиты и добавить валидацию на клиенте.

### 22. Проблема с обработкой race conditions в Home.tsx
**Файл:** `client/src/pages/Home.tsx`
**Проблема:** Использование refs для состояния может привести к race conditions при быстрых изменениях.
**Решение:** Использовать функциональные обновления состояния или более надежный механизм синхронизации.

### 23. Отсутствие проверки на валидность URL в некоторых местах
**Проблема:** В некоторых местах URL валидируются, но не везде последовательно.
**Решение:** Создать единую утилиту для валидации URL и использовать её везде.

### 24. Проблема с логированием чувствительных данных
**Файл:** `server/src/shared/logger.ts`
**Проблема:** Нет явной защиты от логирования паролей, токенов и других чувствительных данных.
**Решение:** Добавить фильтрацию чувствительных полей в логгере.

### 25. Отсутствие проверки на SQL injection (хотя используется параметризация)
**Статус:** ✅ Защита есть через параметризованные запросы
**Примечание:** Все SQL запросы используют параметризацию, что хорошо. Но стоит проверить, что нет мест, где используется конкатенация строк.

## 📝 Рекомендации по улучшению

1. **Добавить интеграционные тесты** для проверки безопасности
2. **Настроить автоматическое сканирование уязвимостей** (например, npm audit, Snyk)
3. **Добавить мониторинг** для отслеживания подозрительной активности
4. **Улучшить обработку ошибок** с более информативными сообщениями
5. **Добавить rate limiting** на все эндпоинты
6. **Улучшить типизацию** - убрать все `any` типы
7. **Добавить валидацию** на всех уровнях (клиент, сервер, БД)
8. **Улучшить логирование** с контекстом и структурой
9. **Добавить метрики** для отслеживания производительности
10. **Создать документацию** по безопасности и best practices

