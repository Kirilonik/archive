# 🚀 Быстрый старт Landing Page

## Установка и запуск

```bash
# 1. Перейдите в папку landing
cd landing

# 2. Установите зависимости
npm install

# 3. Запустите dev сервер
npm run dev
```

Landing page откроется автоматически на `http://localhost:5174`

## Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## Что включено

✅ Минималистичный дизайн в стиле Apple
✅ 3D элементы (Three.js)
✅ Векторная графика с анимациями
✅ Плавные переходы (Framer Motion)
✅ Glassmorphism эффекты
✅ Полностью адаптивный дизайн

## Структура

- `src/components/Hero.tsx` - Главная секция
- `src/components/Features.tsx` - Возможности приложения
- `src/components/Stats.tsx` - Статистика
- `src/components/CTA.tsx` - Призыв к действию
- `src/components/Floating3D.tsx` - 3D элементы
- `src/components/VectorGraphics.tsx` - Векторная графика

## Примечания

- Модуль полностью независим от основного приложения
- Использует отдельный порт (5174) чтобы не конфликтовать с основным приложением
- Все зависимости устанавливаются отдельно в папке `landing/`
